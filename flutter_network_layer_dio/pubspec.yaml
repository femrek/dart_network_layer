name: flutter_network_layer_dio
description: "Dio implementation for flutter_network_layer_core. Uses the dio package to make network requests."
version: 1.0.0-dev3
homepage: https://github.com/femrek/flutter_network_layer/tree/main/flutter_network_layer_dio

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter_network_layer_core: ^1.0.0-dev3
  dio: ^5.7.0
  http_parser: ^4.1.2

dev_dependencies:
  test: ^1.25.14
  http_test_server: ^0.1.1
  very_good_analysis: ^9.0.0
